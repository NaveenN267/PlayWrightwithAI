function name(naveen, ...resu) { // here ... act like a array and it will must append at last
    console.log(naveen);
    console.log(resu);
}
name("Naveen", 1, 2, 3, 2, 1, 5);
name("Namala", "Naveen", "Peddammagadda")
