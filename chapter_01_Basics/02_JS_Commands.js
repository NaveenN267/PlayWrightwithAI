console.log(process.platform);// this is to find the platform(like :wind)
console.log(process.arch); // this is to find the bit(like 32 or 64)
console.log("Node.js version:" + process.version);// this is to find the node.js version
console.log("Hello, World new changes!");